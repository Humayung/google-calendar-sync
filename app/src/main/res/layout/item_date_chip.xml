<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="48dp"
        android:layout_height="64dp"
        android:clickable="true"
        android:minWidth="44dp"
        android:focusable="true">

        <CheckBox
            android:layout_width="0dp"
            android:layout_height="match_parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:checked="false"
            android:id="@+id/card_background"
            android:backgroundTint="@drawable/bg_selectable_container_color_selector"
            android:background="@drawable/bg_rounded_large_container"
            android:button="@null"/>

        <TextView
            android:id="@+id/tv_date"
            style="@style/Text.Header4"
            android:button="@null"
            android:textStyle="bold"
            android:layout_width="0dp"
            android:layout_marginBottom="8dp"
            android:layout_height="wrap_content"
            android:text="21"
            android:enabled="false"
            android:textAlignment="center"
            android:textColor="@drawable/selector_text_color_date_chip"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_day" />

        <TextView
            android:id="@+id/tv_day"
            android:button="@null"
            style="@style/Text.Body1"
            android:layout_width="0dp"
            android:enabled="false"
            android:layout_marginTop="8dp"
            android:layout_height="wrap_content"
            android:text="Mon"
            android:textAlignment="center"
            android:textColor="@drawable/selector_text_color_date_chip"
            app:layout_constraintBottom_toTopOf="@id/tv_date"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
